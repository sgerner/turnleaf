<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The setup UI requires explicit confirmation before accepting an HTTP server. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
