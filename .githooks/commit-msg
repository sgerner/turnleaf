#!/usr/bin/env sh
set -eu

case "${2:-}" in
  merge|squash)
    exit 0
    ;;
esac

ROOT="$(git rev-parse --show-toplevel)"
node "$ROOT/scripts/update-release-notes.mjs" "$1"
git -C "$ROOT" add RELEASE_NOTES.md
